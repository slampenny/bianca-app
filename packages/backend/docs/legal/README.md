# Legal documents (moved)

Canonical legal content lives in **[`packages/legal`](../../legal/)** (`@bianca/legal`).

Run from repo root:

```bash
yarn legal:sync
```

Do not edit copies here — update `packages/legal/*.md` instead.
