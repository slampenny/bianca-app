[general]
enabled = yes
pretty = yes ; Optional
allowed_origins=*

[myphonefriend] ; Your ARI username
type = user
read_only = no ; If your app needs to make changes
password = ${ARI_PASSWORD} ; Ensure this matches what your client uses
; password_format = plain ; If password is plain text